- Группа: УИБО-14-24
- Студент: Мартыненко Даниил
- Вариант: 8 
- Вопрос: 3

---

# Рекурсивное вычисление степени числа

## 🐍 Python

### Как работает алгоритм:
- **Базовый случай**: если **n == 0**, возвращает **1.0**
- **Рекурсивный случай**: возвращает **x * power(x, n - 1)**
- **Процесс для x=2, n=3**:
-- power(2, 3)
# → 2 * power(2, 2)
# → 2 * (2 * power(2, 1))
# → 2 * (2 * (2 * power(2, 0)))
# → 2 * (2 * (2 * 1.0))
# → 8.0

### Чёткая временная сложность:
**O(n)**

### Объяснение временной сложности:
- Выполняется **ровно n рекурсивных вызовов**
- На каждом уровне: **O(1) операций** (сравнение, умножение, вычитание)
- **Общее время**: n × O(1) = O(n)
- **Глубина рекурсии**: O(n) - каждый вызов добавляет фрейм в стек Python
- **Максимальная глубина**: ограничена `sys.getrecursionlimit()` (~1000)

---

## ⚙️ C++

### Как работает алгоритм:
- **Базовый случай**: при n == 0 возвращает 1.0
- **Рекурсивный спуск**: каждый вызов уменьшает n на 1
- **Стек вызовов**: каждый рекурсивный вызов добавляет stack frame
- **Возврат результатов**: вычисления происходят при возврате из рекурсии

### Чёткая временная сложность:
**O(n)**

### Объяснение временной сложности:
- **n рекурсивных вызовов** - каждый с O(1) операциями
- **Операции на уровень**: сравнение, арифметические операции, вызов функции
- **Пространственная сложность**: O(n) из-за стека вызовов
- **Ограничение**: размер стека компилятора (обычно 1-8 МБ)

---

## ☕ Java

### Как работает алгоритм:
- **Базовый случай**: n == 0 → возврат 1.0
- **Рекурсивное вычисление**: x * power(x, n - 1)
- **Стек JVM**: каждый вызов добавляет method frame в стек
- **Постепенное вычисление**: результат накапливается при возврате из рекурсии

### Чёткая временная сложность:
**O(n)**

### Объяснение временной сложности:
- **Количество вызовов**: ровно n раз
- **Время на вызов**: O(1) - примитивные операции
- **Стек вызовов**: O(n) фреймов в стеке JVM
- **Производительность**: быстрые примитивные операции, но риск StackOverflowError

---

## ❓ Почему наивная рекурсивная реализация чисел Фибоначчи неэффективна?

**Ответ:** Наивная рекурсивная реализация Фибоначчи имеет **экспоненциальную сложность O(2ⁿ)** из-за:

1. **Двойной рекурсии**: `fib(n) = fib(n-1) + fib(n-2)`
2. **Повторных вычислений**: одни и те же значения вычисляются многократно
3. **Экспоненциального роста дерева вызовов**
4. **Огромной избыточности вычислений**

**Пример**: `fib(5)` вычисляет `fib(2)` три раза, `fib(3)` два раза.

**Решение**: мемоизация снижает сложность до **O(n)**.
