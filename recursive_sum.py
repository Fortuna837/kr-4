"""
Рекурсивное вычисление суммы элементов массива
Функция последовательно суммирует элементы массива с помощью рекурсии
Базовый случай: достигнут конец массива (index == len(arr))
Рекурсивный случай: текущий элемент + сумма оставшихся элементов
Временная сложность: O(n), где n - количество элементов
"""

def array_sum(arr, index=0):
    # Базовый случай: дошли до конца массива
    if index == len(arr):
        return 0
    # Рекурсивный случай: текущий элемент + сумма оставшихся
    return arr[index] + array_sum(arr, index + 1)

# Тестирование функции
arr = [1, 2, 3, 4, 5]
print(f"Сумма массива {arr} = {array_sum(arr)}")

// Вывод:
// Сумма массива [1, 2, 3, 4, 5] = 15
